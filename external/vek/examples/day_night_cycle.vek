# Day/night cycle example for VEK 2.6.0

state sky = {
  time_of_day:8.0,
  day_length_minutes:18.0
};

fn tick(dt_seconds) {
  sky.time_of_day = (sky.time_of_day + (24.0 / (sky.day_length_minutes * 60.0)) * dt_seconds) % 24.0;
}
