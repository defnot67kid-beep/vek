# v26.8.5 - Clean Build Mode HUD

- Removed the always-visible VehicleBuilder keyboard/command guide panel.
- Suppressed the normal gameplay HUD while Build Mode is active.
- Suppressed the minimap/full-map draw layer while Build Mode is active.
- Kept engineering analysis and VEK part-selection UI visible.
- Gameplay HUD and map return automatically when Build Mode is exited.
- No signed `.vek` gameplay scripts were changed.
