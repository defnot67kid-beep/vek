[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "vek-runtime"
version = "1.7.0"
description = "Python ctypes binding for VEK"
requires-python = ">=3.9"
license = {text = "MIT"}
