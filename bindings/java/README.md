# VEK Java binding

This binding uses the final Foreign Function & Memory API in Java 22+ and calls
VEK's stable C ABI directly. Point `VekRuntime` at `vek.dll`, `libvek.so`, or
`libvek.dylib`. No second VEK interpreter is implemented in Java.
